# csv-answer-generator
generate_csv_answer_automation

## 환경 정보
```
pip install pandas
pip install tqdm
pip install google-genai

pip freeze > requirements.txt
```

## 환경 세팅
```bash
cd csv-answer-generator
python3 -m venv .venv
source .venv/bin/activate

pip install -r requirements.txt
```
### 파일 실행

```bash
python3 -m make_csv_results
```

## 실행 방법

### input 데이터 파일 위치
data/input/

### output 데이터 파일 위치
data/output/



